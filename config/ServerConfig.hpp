#ifndef SERVERCONFIG_HPP
#define SERVERCONFIG_HPP

#include <string>
#include <vector>

class ServerConfig
{
private:
/*
	location
*/
public:
	ServerConfig();
	ServerConfig(const ServerConfig &);
	~ServerConfig();
};

#endif /* SERVERCONFIG_HPP */
